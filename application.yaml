apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: testApp-argo-application
  namespace: argocd
spec:
  project: default

  source:
    repoURL: https://github.com/MaksymLeus/testApp_Configuration.git
    targetRevision: HEAD
    path: dev
  destination: 
    server: https://kubernetes.default.svc
    namespace: testApp

  syncPolicy:
    syncOptions:
    - CreateNamespace=true

    automated:
      selfHeal: true
      prune: true